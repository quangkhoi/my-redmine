name: Build branch

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show branch
        run: |
          echo "Branch: ${{ github.ref_name }}"
          git log -1 --oneline

      - name: Build
        run: |
          echo "Build started"
